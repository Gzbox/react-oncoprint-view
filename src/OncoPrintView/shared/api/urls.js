export function getSampleViewUrl(studyId, sampleId, navIds) {
  console.log('getSampleViewUrl', studyId, sampleId, '跳转到样本详情页面');
}
export function getPatientViewUrl(studyId, caseId, navIds) {
  console.log(
    'getPatientViewUrl',
    studyId,
    caseId,
    navIds,
    '跳转到患者详情页面',
  );
}
